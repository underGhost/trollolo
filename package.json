{
  "name": "trollolo",
  "version": "1.0.0",
  "description": "Troll websites",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && babel src --out-dir build",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "troll"
  ],
  "bin": {
    "trollolo": "index.js"
  },
  "author": "underghost",
  "license": "ISC",
  "dependencies": {
    "nightmare": "^2.8.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
